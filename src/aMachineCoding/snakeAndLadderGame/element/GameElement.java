package aMachineCoding.snakeAndLadderGame.element;

public interface GameElement {
    int getStartPosition();     // Where the element is placed

    int getDestination();       // Where the element sends the player
}
